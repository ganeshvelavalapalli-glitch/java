class Solution {
    public int missingNumber(int[] nums) {
        int s=(nums.length*(nums.length+1))/2;
        int x=0;
        for(int i=0;i<nums.length;i++)
        {
            x=x+nums[i];
        }
        return s-x;
    }
}
